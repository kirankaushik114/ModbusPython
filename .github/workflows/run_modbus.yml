name: Run Modbus Automation

on:
  schedule:
    - cron: "0 * * * *"  # Runs every hour
  workflow_dispatch:      # Allows manual run

jobs:
  run-modbus:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Modbus orchestrator (server → client → stop)
        run: |
          python run_modbus.py

      - name: Save logs as artifact
        if: always()
        run: |
          mkdir -p logs
          echo "Modbus workflow completed at $(date)" > logs/run_summary.txt
        # Upload logs as downloadable artifact
      - name: Upload log artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: modbus-run-logs
          path: logs/
